import { TranslatorPipePipe } from './translator-pipe.pipe';

describe('TranslatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
