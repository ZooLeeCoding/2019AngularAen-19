import { PrivilegeNPipe } from './privilege-n.pipe';

describe('PrivilegeNPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivilegeNPipe();
    expect(pipe).toBeTruthy();
  });
});
